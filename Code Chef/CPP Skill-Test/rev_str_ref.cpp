#include <iostream>
#include <string>
using namespace std;

// Function to reverse the string using call by reference
void reverseString(string &s) {
    // complete the function
}

int main() {
    string str;
    // Read input string
    cin >> str;

    // Call reverseString function to reverse the string
    reverseString(str);

    // Output the reversed string
    cout << str << endl;

    return 0;
}
